export interface Travel {
  id_travel?: number; // with ? the property is optional
  country: string;
  city: string;
  date: string;
  budget: string;
  comment: string;
  contact: string;
  active: boolean;
}
